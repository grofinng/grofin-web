[build]
  command = "npm run build"
  publish = "build"

[build.environment]
  NODE_VERSION = "20"

# SPA fallback so direct loads of /apply/:id, /admin, etc. don't 404
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
